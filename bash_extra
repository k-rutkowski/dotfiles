set -o vi

PATH=$HOME/.local/bin:$HOME/.bin:$HOME/bin:$HOME/scripts:$HOME/.local/share/bob/nvim-bin:$PATH

eval "$(starship init bash)"
eval "$(zoxide init bash)"

if [ -f ~/.bash_secret ]; then
    . ~/.bash_secret
fi
